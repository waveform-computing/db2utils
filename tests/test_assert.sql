CALL ASSERT_SIGNALS('70001', 'CALL SIGNAL_STATE(''70001'', ''Foo'')')!

-- For some bizarre reason, ASSERT_SIGNALS doesn't like being nested; DB2
-- 9.7FP8 bizarrely complains about a non-existent SAVEPOINT (probably a bug)
-- so again we use a stored proc to test things here
CREATE PROCEDURE TEST_ASSERT()
    SPECIFIC TEST_ASSERT
    NO EXTERNAL ACTION
BEGIN ATOMIC
    DECLARE EXIT HANDLER FOR SQLSTATE '90001' BEGIN END;
    CALL ASSERT_SIGNALS('70001', 'CALL SIGNAL_STATE(''70000'', ''Foo'')');
END!

CALL TEST_ASSERT()!

CREATE OR REPLACE PROCEDURE TEST_ASSERT()
    SPECIFIC TEST_ASSERT
    NO EXTERNAL ACTION
BEGIN ATOMIC
    DECLARE EXIT HANDLER FOR SQLSTATE '90001' BEGIN END;
    CALL ASSERT_SIGNALS('70001', 'VALUES CURRENT DATE');
END!

CALL TEST_ASSERT()!

-- We can't execute SELECT or VALUES from EXECUTE IMMEDIATE so again we wrap it
-- in a procedure which we can call from EXECUTE IMMEDIATE. The procedure
-- simply takes some SQL, bungs it on the end of a VALUES statement and
-- executes it, expecting to get a single integer back in return, which is the
-- calling convention of all the ASSERT_* functions
CREATE PROCEDURE TEST_VALUES(SQL CLOB(64K))
    SPECIFIC TEST_VALUES
    NO EXTERNAL ACTION
    DYNAMIC RESULT SETS 1
BEGIN ATOMIC
    DECLARE R INTEGER;
    DECLARE S STATEMENT;
    DECLARE C CURSOR FOR S;
    PREPARE S FROM 'VALUES ' || SQL;
    OPEN C;
    FETCH C INTO R;
END!

VALUES ASSERT_EQUALS(0, 0)!
VALUES ASSERT_EQUALS(0.0, 0.0)!
VALUES ASSERT_EQUALS(CURRENT DATE, CURRENT DATE)!
VALUES ASSERT_EQUALS(CURRENT TIMESTAMP, CURRENT TIMESTAMP)!
VALUES ASSERT_EQUALS(CURRENT TIME, CURRENT TIME)!
VALUES ASSERT_EQUALS('foo', 'foo')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_EQUALS(0, 1)'')')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_EQUALS(0.0, 1.0)'')')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_EQUALS(CURRENT DATE, CURRENT DATE - 1 DAY)'')')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_EQUALS(CURRENT TIMESTAMP, CURRENT TIMESTAMP + 1 MICROSECOND)'')')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_EQUALS(CURRENT TIME, CURRENT TIME - 1 HOUR)'')')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_EQUALS(''''foo'''', ''''bar'''')'')')!

VALUES ASSERT_NOT_EQUALS(0, 1)!
VALUES ASSERT_NOT_EQUALS(0.0, 1.0)!
VALUES ASSERT_NOT_EQUALS(CURRENT DATE, CURRENT DATE - 1 DAY)!
VALUES ASSERT_NOT_EQUALS(CURRENT TIMESTAMP, CURRENT TIMESTAMP + 1 MICROSECOND)!
VALUES ASSERT_NOT_EQUALS(CURRENT TIME, CURRENT TIME - 1 HOUR)!
VALUES ASSERT_NOT_EQUALS('foo', 'bar')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_NOT_EQUALS(0, 0)'')')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_NOT_EQUALS(0.0, 0.0)'')')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_NOT_EQUALS(CURRENT DATE, CURRENT DATE)'')')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_NOT_EQUALS(CURRENT TIMESTAMP, CURRENT TIMESTAMP)'')')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_NOT_EQUALS(CURRENT TIME, CURRENT TIME)'')')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_NOT_EQUALS(''''foo'''', ''''foo'''')'')')!

VALUES ASSERT_IS_NULL(CAST(NULL AS INTEGER))!
VALUES ASSERT_IS_NULL(CAST(NULL AS DOUBLE))!
VALUES ASSERT_IS_NULL(CAST(NULL AS DATE))!
VALUES ASSERT_IS_NULL(CAST(NULL AS TIMESTAMP))!
VALUES ASSERT_IS_NULL(CAST(NULL AS TIME))!
VALUES ASSERT_IS_NULL(CAST(NULL AS VARCHAR(10)))!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_IS_NULL(1)'')')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_IS_NULL(1.0)'')')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_IS_NULL(CURRENT DATE)'')')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_IS_NULL(CURRENT TIMESTAMP)'')')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_IS_NULL(CURRENT TIME)'')')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_IS_NULL(''''foo'''')'')')!

VALUES ASSERT_IS_NOT_NULL(1)!
VALUES ASSERT_IS_NOT_NULL(1.0)!
VALUES ASSERT_IS_NOT_NULL(CURRENT DATE)!
VALUES ASSERT_IS_NOT_NULL(CURRENT TIMESTAMP)!
VALUES ASSERT_IS_NOT_NULL(CURRENT TIME)!
VALUES ASSERT_IS_NOT_NULL('foo')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_IS_NOT_NULL(CAST(NULL AS INTEGER))'')')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_IS_NOT_NULL(CAST(NULL AS DOUBLE))'')')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_IS_NOT_NULL(CAST(NULL AS DATE))'')')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_IS_NOT_NULL(CAST(NULL AS TIMESTAMP))'')')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_IS_NOT_NULL(CAST(NULL AS TIME))'')')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL TEST_VALUES(''ASSERT_IS_NOT_NULL(CAST(NULL AS VARCHAR(10)))'')')!

CREATE TABLE FOO (I INTEGER NOT NULL)!
CREATE TRIGGER FOO_INSERT
    AFTER INSERT ON FOO
    FOR EACH ROW
BEGIN ATOMIC
    SIGNAL SQLSTATE '70000' SET MESSAGE_TEXT = 'You cannot insert to this table';
END!
CREATE PROCEDURE FOO_INSERT(I INTEGER)
    LANGUAGE SQL
    SPECIFIC FOO_INSERT1
    MODIFIES SQL DATA
    NO EXTERNAL ACTION
BEGIN
    INSERT INTO FOO(I) VALUES(I);
END!

CALL ASSERT_TABLE_EXISTS('FOO')!
CALL ASSERT_TABLE_EXISTS(CURRENT SCHEMA, 'FOO')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL ASSERT_TABLE_EXISTS(''BAR'')')!

CALL ASSERT_COLUMN_EXISTS('FOO', 'I')!
CALL ASSERT_COLUMN_EXISTS(CURRENT SCHEMA, 'FOO', 'I')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL ASSERT_COLUMN_EXISTS(''FOO'', ''J'')')!

CALL ASSERT_TRIGGER_EXISTS('FOO_INSERT')!
CALL ASSERT_TRIGGER_EXISTS(CURRENT SCHEMA, 'FOO_INSERT')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL ASSERT_TRIGGER_EXISTS(''BAR'')')!

CALL ASSERT_ROUTINE_EXISTS('FOO_INSERT')!
CALL ASSERT_ROUTINE_EXISTS(CURRENT SCHEMA, 'FOO_INSERT')!
CALL ASSERT_SIGNALS(ASSERT_FAILED_STATE, 'CALL ASSERT_ROUTINE_EXISTS(''BAR'')')!

DROP TABLE FOO!
DROP SPECIFIC PROCEDURE TEST_VALUES!
DROP SPECIFIC PROCEDURE TEST_ASSERT!

-- vim: set et sw=4 sts=4:
